# Code Style & Conventions
- Kotlin code uses explicit comments with WHY/NOTE tags for non-obvious decisions.
- Debug logging is commonly guarded by `BuildConfig.DEBUG` (e.g., `DEBUG_LOGS`).
- Japanese comments are present; keep UTF-8 encoding for files with Japanese text.
