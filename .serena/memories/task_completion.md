# Task Completion Checklist
- If code or config changes are made, run appropriate Flutter commands (e.g., `flutter analyze`, `flutter test`) when applicable.
- If only documentation changes are made, note that tests may be skipped.
- Update docs when behavior or debug logging format changes.
