# Suggested Commands (Windows / PowerShell)
- `flutter pub get` — fetch dependencies.
- `flutter run` — run on a connected Android device/emulator.
- `flutter analyze` — static analysis (not specified in repo docs, but standard for Flutter).
- `flutter test` — run tests if/when added.
